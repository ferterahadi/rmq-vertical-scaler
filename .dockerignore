# Git / CI
.git
.gitignore
.github

# Node v1 sources (not used by the Go build)
node_modules
package.json
package-lock.json
webpack.config.js
lib
bin

# Build output
dist
build

# Docs and generated deployment artifacts (not needed in the image).
# NOTE: examples/ and schema/ are Go packages compiled into the binary since
# v2.1.0 (embedded config schema + example templates) — do not ignore them.
*.md
*-scaler.yaml
docs
scripts
tests

# OS / IDE
.DS_Store
.vscode
.idea
*.swp
*.swo

# Docker
Dockerfile
.dockerignore
